from lib.models import svd, als, kernelNet, autoencoder  # , fm, fm_rel

models = {'svd': svd,
          'als': als,
          'kernelNet': kernelNet,
          'autoencoder': autoencoder
          # 'fm': fm,
          # 'fm_rel': fm_rel
          }
